# Prefabs

Our game prefabs: blades, sliceable object(s), potion flask, enemy skeleton, combat HUD, scoreboard,
combat rig, town `Start Match` interaction, etc. Combat object prefabs are pooled via
`XRMultiplayer.Pooler` subclasses.
